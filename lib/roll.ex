defmodule Roll do
  @moduledoc """
  Roll is a tool intended for use in executing
  sql migrations that move data between tables and
  that are only used to make a refactor.

  It is usefull when you need to move data and each
  migration is a migration forward.
  """
end
