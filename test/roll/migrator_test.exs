defmodule Roll.MigratorTest do
  use ExUnit.Case
  # import Support.FileHelpers
  #  alias Roll.Migrator
  #  alias Support.Repo

  #  @migrations_path Path.join([tmp_path(), inspect(Roll.Migrate), "roll"])

  test "call migrator.run/4" do
    # TODO: Añadir pruebas con repositorio
  end
end
